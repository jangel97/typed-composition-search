\appendix

\section{Sample Queries by Category}
\label{sec:appendix-queries}

Table~\ref{tab:sample-queries} provides representative queries for each of the six benchmark categories.

\begin{table*}[t]
\centering
\small
\begin{tabular}{l p{6.5cm} l l}
\toprule
\textbf{Category} & \textbf{Query} & \textbf{Types} & \textbf{Expected Tools} \\
\midrule
Clean & Get the logs for pods running the nginx deployment in the production namespace & Namespace $\rightarrow$ PodLogs & \texttt{list\_pods}, \texttt{select\_pod}, \texttt{get\_pod\_logs} \\
\addlinespace
Multi-hop & What node is running the latest pod created by the api deployment? & Deployment $\rightarrow$ Node & \texttt{get\_deployment\_pods}, \texttt{select\_pod}, \texttt{get\_pod\_node} \\
\addlinespace
Noisy & Something seems wrong with the API. Can you figure out where it is running? & Deployment $\rightarrow$ Node & \texttt{get\_deployment\_pods}, \texttt{select\_pod}, \texttt{get\_pod\_node} \\
\addlinespace
Synonym & How many replicas does the auth workload have? & Deployment $\rightarrow$ HPA & \texttt{get\_deployment\_hpa} \\
\addlinespace
Ambiguous & Is the checkout service running? & Service $\rightarrow$ Endpoints & \texttt{get\_endpoints} \\
\addlinespace
Multi-path & Are all checks passing on the latest commit? & Commit $\rightarrow$ Status & \texttt{get\_commit\_statuses}, \texttt{select\_status} \\
\bottomrule
\end{tabular}
\caption{Sample queries for each benchmark category. Types shows the ground-truth source $\rightarrow$ target entity types. All examples are from the Kubernetes or GitHub domains.}
\label{tab:sample-queries}
\end{table*}

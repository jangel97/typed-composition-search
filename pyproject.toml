[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "typed-composition-search"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.89.3",
]

[dependency-groups]
dev = ["pytest", "openai"]
